### Короткий ответ  
Шаг  
```math
x_{t+1}=x_t-\gamma\,\tanh(g_t),\qquad g_t=\nabla f(x_t)
```  
является шагом наискорейшего спуска по **сглаженной 𝑳∞-норме** (логистически-сглаженный вариант нормы Чебышёва) и одновременно точным шагом зеркального спуска, построенного на **логистической (двоично-энтропийной) Брегмановой дивергенции**  

---

### 1. Интерпретация как «классический» steepest descent  

Для любой нормы ‖·‖ направление наискорейшего спуска определяется решением  

```math
d_t=\arg\min_{\|d\|\le 1}\ \langle g_t,d\rangle,\qquad x_{t+1}=x_t-\gamma d_t .
```  

• Если взять обычную 𝑳∞-норму, то решением будет вектор `−sign(g_t)` (координаты ±1).  
• Чтобы вместо жёсткого `sign` получить гладкую функцию, вводят *сглаженную* норму с единичным «шаром»  

```math
\mathcal B=\Bigl\{d\in\mathbb R^n:\; \sum_{i=1}^{n}\ln\!\bigl(2\cosh(d_i)\bigr)\le 1\Bigr\}.
```  

Её полярный (дуальный) «шар» описывается неравенством  
```math
\sum_{i=1}^{n}\tfrac12\!\bigl[(1+v_i)\ln(1+v_i)+(1-v_i)\ln(1-v_i)\bigr]\le 1 ,
```  
а оптимизатор имеет координаты  

```math
d_{t,i}=-\tanh(g_{t,i}) .
```  

Тем самым ваш шаг это steepest-descent по указанной «логистически-сглаженной 𝑳∞-норме» (размер шага γ задаёт радиус).

---

### 2. Интерпретация как зеркальный спуск  

Зеркальный спуск задаётся потенциальной функцией ψ и её Брегмановой дивергенцией  

```math
D_{\psi}(x,y)=\psi(x)-\psi(y)-\langle\nabla\psi(y),x-y\rangle .
```  

Выберем покоординатный потенциал  

```math
\psi(z)=\sum_{i=1}^{n}\ln\!\bigl(2\cosh(z_i)\bigr) .
```  

Тогда  
```math
\nabla\psi(z)=\tanh(z),\qquad (\nabla\psi)^{-1}(u)=\operatorname{artanh}(u) .
```  

Шаг зеркального спуска с этим ψ выглядит так  

```math
x_{t+1}=\tanh\!\bigl(\operatorname{artanh}(x_t)-\gamma\,g_t\bigr) .
```  

Если координаты x_t лежат достаточно близко к 0 (или мы линейно приближаем tanh в точке artanh(x_t)), то выражение упрощается до  

```math
x_{t+1}\approx x_t-\gamma\,\tanh(g_t) ,
```  

что в точности совпадает с вашей формулой. Поэтому ваш алгоритм можно трактовать как зеркальный спуск с логистической дивергенцией  

```math
D_{\text{log}}(x,y)=\sum_{i=1}^{n}
      \tfrac12\!\Bigl[(1+x_i)\ln\frac{1+x_i}{1+y_i}
                     +(1-x_i)\ln\frac{1-x_i}{1-y_i}\Bigr] .
```  

Эта дивергенция является «двоичной» Kullback–Leibler и естественно ограничивает каждую координату |x_i|<1, что и объясняет насыщение tanh.

---

### 3. Интуитивная геометрия  

• При маленьких |g_i|: tanh(g_i)≈g_i, и шаг совпадает с обычным евклидовым спуском.  
• При больших |g_i| градиент «обрубает»-ся до ±1, так что каждую координату нельзя сдвинуть больше чем на γ за один шаг — это эквивалент trust-region по 𝑳∞.  
• В зеркальной интерпретации движение идёт линейным шагом в дуальных координатах y=artanh(x) (их метрика задаётся Fisher-информацией Бернулли-семейства), после чего картируется обратно через tanh в исходное пространство.

---

### Итог  

Шаг `x_{t+1}=x_t-γ tanh(g_t)` — это:  
1. steepest descent по сглаженной 𝑳∞-норме (логистическая аппроксимация нормы Чебышёва);  
2. эквивалентный зеркальный спуск с потенциалом ψ(z)=∑ ln(2 cosh z) и Брегмановой логистической дивергенцией, часто называемой «двоичной энтропийной».